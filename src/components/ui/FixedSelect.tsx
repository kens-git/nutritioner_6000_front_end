import Select from "./Select";

// represents a select input that turns into a fixed label after selection
const FixedSelect: React.FC<{}> = (props) => {
  return (
    <Select /> // && or label
  ); // TODO: trailing semi-colons?
}

export default FixedSelect;
